Noteworhy items:

TextEdit.java 
     This class extends the basic EditText native in android and adds the abilty to format selected parts of text with Bold/Italic/Underline.

StoryListActivity @ StoryListAdapter 
    These classes are an example of how to consume a web API via async thread, parse the json into a data model & use the array adapter to bind to the view
